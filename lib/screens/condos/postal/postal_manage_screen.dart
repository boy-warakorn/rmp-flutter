import 'package:flutter/material.dart';

class PostalManageScreen extends StatelessWidget {
  const PostalManageScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
