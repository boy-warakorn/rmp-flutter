import 'package:flutter/material.dart';

class PostalScreen extends StatelessWidget {
  const PostalScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('Welcome Postal'));
  }
}
